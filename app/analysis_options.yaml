include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Freezed 3.0 uses @JsonKey on constructor params — standard pattern
    invalid_annotation_target: ignore

linter:
  rules:
    # avoid_print: false
    # prefer_single_quotes: true
